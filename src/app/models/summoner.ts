export class Summoner {
  profileIconId: number;
  name: string;
  summonerLevel: number;
  accountId: number;
  id: number;
  revisionDate: number;
  // can be got only per api call
  private constructor() {}
}
